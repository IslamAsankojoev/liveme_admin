import * as React from "react";import {ImageField, ImageInput, useEditContext, useRecordContext} from "react-admin";const ImageInputCustom = (props) => {    const [newImage, setNewImage] = React.useState(false);    const product = useRecordContext();    const { record, isLoading } = useEditContext();    console.log(product)    console.log(record)    return (        <>            <ImageInput {...props} onChange={()=>{setNewImage(true)}} >                <ImageField source={"src"} />            </ImageInput>            {!newImage && <ImageField source={'image'} />}        </>    );}export default ImageInputCustom;