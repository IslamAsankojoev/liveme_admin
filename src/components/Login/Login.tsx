// in src/MyLoginPage.jsimport {Button, TextField } from '@mui/material';import * as React from 'react';import { useState } from 'react';import { useLogin, useNotify } from 'react-admin';import style from './Login.module.scss';import VisibilityIcon from '@mui/icons-material/Visibility';import VisibilityOffIcon from '@mui/icons-material/VisibilityOff';const Login = () => {    const [username, setUsername] = useState('');    const [password, setPassword] = useState('');    const [showPassword, setShowPassword] = useState(false);    const login = useLogin();    const notify = useNotify();    const handleSubmit = (e:any) => {        e.preventDefault();        login({ username, password }).catch(() =>            notify('Invalid email or password')        );    };    return (        <div className={style.loginPage}>        <form onSubmit={handleSubmit}>            <h3>Войти в админ панель</h3>            <TextField type={'text'} label="Username" variant="outlined" value={username} onChange={e => setUsername(e.target.value)}/>            <div className={style.passwordInput}>                <TextField type={                    showPassword ? 'text' : 'password'                } label="Password" variant="outlined" value={password} onChange={e => setPassword(e.target.value)}/>                {showPassword ? <VisibilityIcon className={style.eye} onClick={() => setShowPassword(!showPassword)}/> : <VisibilityOffIcon className={style.eye} onClick={() => setShowPassword(!showPassword)}/>}            </div>            <Button type={'submit'} variant="contained" sx={{                marginTop: '10px',            }}>Войти</Button>        </form>        </div>    );};export default Login;